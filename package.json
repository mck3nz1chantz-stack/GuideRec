{
  "name": "guiderec",
  "private": true,
  "version": "0.4.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.mjs",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=guiderec --commit-dirty=true"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.58.0",
    "vite": "^7.0.4",
    "wrangler": "^4.137.0"
  }
}